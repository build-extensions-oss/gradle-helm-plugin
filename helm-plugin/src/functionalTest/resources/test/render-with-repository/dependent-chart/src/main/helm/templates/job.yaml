apiVersion: batch/v1
kind: Job
metadata:
  name: {{ .Chart.Name }}-job
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: job
          image: busybox:1.37
          command: ["sh", "-c", "echo dependent chart"]
