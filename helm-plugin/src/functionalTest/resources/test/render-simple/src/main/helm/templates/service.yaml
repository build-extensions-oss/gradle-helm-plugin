apiVersion: v1
kind: Service
metadata:
  name: xxxxx{{ .Chart.Name }}
spec:
  type: ignored
  ports:
    - port: 123
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: something